general {
    output_format = "i3bar"
    colors = true
    interval = 5
}

order += "volume master"

volume master {
    format = "| S: %volume"
    format_muted = "| S: muted"
    device = "pulse"
}
