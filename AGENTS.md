For any file search or grep in the current git-indexed directory, use the fff MCP tools (`grep`, `multi_grep`, and `find_files`). Do not look for or invoke a shell command named `fff`.
Do NOT write tests or think about backwards compatibility at all unless the user asks for them.

If I mention vps or tell you to use vps, you can access it using `ssh vps`.

When something doesnt work, dont add a fallback, fix the underlying issue.
